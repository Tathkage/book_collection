<p style="color: green"><%= notice %></p>

<h1>Books</h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to "Show Book Details", book %>
    </p>
  <% end %>
</div>

<%= link_to "Back to Home Page", user_books_path %>